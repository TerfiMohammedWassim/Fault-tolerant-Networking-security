# ═══════════════════════════════════════════════════════════════════════════
# PLAYBOOK AUTONOME — SW3 (Cumulus VX 4.x)
# Rôle : Core switch L3 — routage inter-VLAN par routes statiques
# Usage : ansible-playbook -i "IP_SW3," sw3.yml -u cumulus -k --become
# ═══════════════════════════════════════════════════════════════════════════
---
- name: Configuration complète SW3
  hosts: sw3
  gather_facts: false
  become: true

  vars:
    # ── Identité système ──────────────────────────────────────────────────
    hostname: sw3
    loopback_ip: "10.10.10.3/32"

    # ── Uplinks depuis SW1 et SW2 ─────────────────────────────────────────
    uplink_ports:
      - swp1    # vers SW1 swp5
      - swp2    # vers SW2 swp5

    # ── Bridge ────────────────────────────────────────────────────────────
    bridge:
      name: bridge
      vids: "10 20" # Cumulus préfère l'espace ou la virgule sans guillemets complexes
      pvid: 1

    # ── SVIs — interfaces L3 par VLAN ─────────────────────────────────────
    svi_interfaces:
      - vlan_id: 10
        ip_address: "192.168.1.254/24"
        description: "Passerelle VLAN10 — Machine1"
      - vlan_id: 20
        ip_address: "192.168.2.254/24"
        description: "Passerelle VLAN20 — Machine2"

    # ── Routes statiques inter-VLAN ───────────────────────────────────────
    static_routes:
      - prefix: "192.168.1.0/24"
        dev: vlan10
      - prefix: "192.168.2.0/24"
        dev: vlan20

    # ── Sysctl ────────────────────────────────────────────────────────────
    sysctl_settings:
      - { name: "net.ipv4.ip_forward", value: "1" }
      - { name: "net.ipv4.conf.all.rp_filter", value: "0" }
      - { name: "net.ipv4.conf.default.rp_filter", value: "0" }
      - { name: "net.core.netdev_max_backlog", value: "1000" }

  tasks:
    # ════════════════════════════════════════════════════════════════════
    # 1. PARAMÈTRES SYSTÈME
    # ════════════════════════════════════════════════════════════════════

    - name: "[SYS] Définir le hostname"
      hostname:
        name: "{{ hostname }}"

    - name: "[SYS] Appliquer les paramètres sysctl"
      sysctl:
        name: "{{ item.name }}"
        value: "{{ item.value }}"
        state: present
        reload: true
        sysctl_file: /etc/sysctl.d/99-campus.conf
      loop: "{{ sysctl_settings }}"

    # ════════════════════════════════════════════════════════════════════
    # 2. INTERFACES RÉSEAU (SVIs + bridge)
    # ════════════════════════════════════════════════════════════════════

    - name: "[NET] Déployer /etc/network/interfaces"
      copy:
        dest: /etc/network/interfaces
        owner: root
        group: root
        mode: '0644'
        content: |
          # /etc/network/interfaces — {{ hostname }}
          # Généré par Ansible — {{ lookup('pipe', 'date') }}

          auto lo
          iface lo inet loopback
              address {{ loopback_ip }}

          auto eth0
          iface eth0 inet dhcp

          # ── Uplinks (Trunks) ──────────────────────────────────────────
          {% for port in uplink_ports %}
          auto {{ port }}
          iface {{ port }}
          {% endfor %}

          # ── Bridge VLAN-aware ────────────────────────────────────────
          auto {{ bridge.name }}
          iface {{ bridge.name }}
              bridge-vlan-aware yes
              bridge-ports {{ uplink_ports | join(' ') }}
              bridge-vids {{ bridge.vids }}
              bridge-pvid {{ bridge.pvid }}

          # ── SVIs (Interfaces VLAN) ───────────────────────────────────
          {% for svi in svi_interfaces %}
          # {{ svi.description }}
          auto vlan{{ svi.vlan_id }}
          iface vlan{{ svi.vlan_id }}
              address {{ svi.ip_address }}
              vlan-id {{ svi.vlan_id }}
              vlan-raw-device {{ bridge.name }}

          {% endfor %}

    - name: "[NET] Recharger les interfaces"
      command: ifreload -a
      changed_when: true

    # ════════════════════════════════════════════════════════════════════
    # 3. VÉRIFICATIONS (Tests)
    # ════════════════════════════════════════════════════════════════════

    - name: "[CHECK] Vérifier ip_forward"
      command: sysctl net.ipv4.ip_forward
      register: ip_fwd
      changed_when: false

    - name: "[CHECK] Confirmer IP sur SVIs"
      command: "ip addr show vlan{{ item.vlan_id }}"
      register: svi_check
      changed_when: false
      loop: "{{ svi_interfaces }}"

    - name: "[CHECK] Résumé final"
      debug:
        msg:
          - "Host: {{ hostname }}"
          - "Forwarding: {{ ip_fwd.stdout }}"
          - "VLANs: {{ bridge.vids }}"
