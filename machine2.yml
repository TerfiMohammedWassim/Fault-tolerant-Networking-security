# ═══════════════════════════════════════════════════════════════════════════
# PLAYBOOK AUTONOME — Machine2 (Debian 12 minimal)
# Rôle : Hôte avec bond LACP dual-homed vers SW1 (swp1) et SW2 (swp2)
# Interfaces : ens33 → LAN1 → SW1 swp1
#              ens34 → LAN2 → SW2 swp2
#              ens35 →  NAT → gestion Ansible (OOB)
# Usage : ansible-playbook machine2.yml -u debian -k --become
# ═══════════════════════════════════════════════════════════════════════════
---
- name: Configuration complète Machine2
  hosts: machine2
  gather_facts: true
  become: true

  vars:

    bond:
      name: bond1
      ip: "192.168.2.1/24"
      gateway: "192.168.2.254"       # SVI VLAN20 sur SW3/SW4
      members:
        - ens33                      # → SW1 swp1 (LAN segment 1)
        - ens34                      # → SW2 swp2 (LAN segment 2)
      mode: "802.3ad"
      miimon: 100
      min_links: 1
      xmit_hash_policy: "layer3+4"

    mgmt_iface: ens35                # NAT OOB — hors bond, conservé intact

    static_routes:
      - prefix: "192.168.1.0/24"
        gateway: "192.168.2.254"
        description: "Réseau Machine1 via SW3/SW4"
      - prefix: "10.10.10.0/24"
        gateway: "192.168.2.254"
        description: "Loopbacks switches via SW3/SW4"

    required_packages:
      - ifenslave
      - ethtool
      - net-tools
      - iproute2
      - tcpdump
      - traceroute

    kernel_modules:
      - bonding

    sysctl_settings:
      - { name: net.ipv4.ip_forward,  value: "0" }
      - { name: net.core.rmem_max,    value: "134217728" }
      - { name: net.core.wmem_max,    value: "134217728" }

  tasks:

    # ════════════════════════════════════════════════════════════════════
    # 1. PARAMÈTRES SYSTÈME
    # ════════════════════════════════════════════════════════════════════


    - name: "[SYS] Installer les paquets requis"
      apt:
        name: "{{ item }}"
        state: present
        update_cache: false
      loop: "{{ required_packages }}"
      register: pkg_result
      retries: 2
      delay: 5
      until: pkg_result is succeeded

    - name: "[SYS] Charger les modules kernel"
      modprobe:
        name: "{{ item }}"
        state: present
      loop: "{{ kernel_modules }}"

    - name: "[SYS] Rendre les modules persistants"
      lineinfile:
        path: /etc/modules
        line: "{{ item }}"
        state: present
      loop: "{{ kernel_modules }}"

    - name: "[SYS] Appliquer les paramètres sysctl"
      sysctl:
        name: "{{ item.name }}"
        value: "{{ item.value }}"
        state: present
        reload: true
        sysctl_file: /etc/sysctl.d/99-campus.conf
      loop: "{{ sysctl_settings }}"

    # ════════════════════════════════════════════════════════════════════
    # 2. INTERFACES RÉSEAU — BOND LACP
    # ════════════════════════════════════════════════════════════════════

    - name: "[NET] Sauvegarder l'ancien fichier interfaces"
      copy:
        src: /etc/network/interfaces
        dest: "/etc/network/interfaces.bak.{{ lookup('pipe', 'date +%Y%m%d_%H%M%S') }}"
        remote_src: true
      ignore_errors: true

    - name: "[NET] Descendre les interfaces membres"
      command: "ip link set {{ item }} down"
      loop: "{{ bond.members }}"
      ignore_errors: true
      changed_when: true

    - name: "[NET] Supprimer le bond existant si présent"
      shell: |
        ip addr flush dev ens33 || true
        ip addr flush dev ens34 || true
        ip link set {{ bond.name }} down || true
        ip link delete {{ bond.name }} || true
      failed_when: false

    - name: "[NET] Déployer /etc/network/interfaces"
      copy:
        dest: /etc/network/interfaces
        owner: root
        group: root
        mode: '0644'
        content: |
          # Généré par Ansible — {{ lookup('pipe', 'date') }}
          # Bond LACP 802.3ad dual-homed
          #   ens33 → SW1 swp1 (LAN segment 1)
          #   ens34 → SW2 swp2 (LAN segment 2)
          # ens35 réservé à la gestion Ansible — ne pas toucher

          auto lo
          iface lo inet loopback

          # ── Gestion OOB (NAT) — DHCP ───────────────────────────
          auto {{ mgmt_iface }}
          iface {{ mgmt_iface }} inet dhcp

          # ── Membres du bond ──────────────────────────────────────────
          {% for member in bond.members %}
          # {{ member }} → SW{{ loop.index }} swp{{ loop.index }}
          auto {{ member }}
          iface {{ member }} inet manual
              bond-master {{ bond.name }}

          {% endfor %}
          # ── Bond LACP 802.3ad ────────────────────────────────────────
          auto {{ bond.name }}
          iface {{ bond.name }} inet static
              address {{ bond.ip }}
              gateway {{ bond.gateway }}
              bond-slaves {{ bond.members | join(' ') }}
              bond-mode {{ bond.mode }}
              bond-miimon {{ bond.miimon }}
              bond-min-links {{ bond.min_links }}
              bond-xmit-hash-policy {{ bond.xmit_hash_policy }}
              {% for route in static_routes %}
              post-up ip route add {{ route.prefix }} via {{ bond.gateway }} dev {{ bond.name }} || true
              {% endfor %}

    - name: "[NET] Monter toutes les interfaces"
      command: ifup -a --exclude=lo --exclude={{ mgmt_iface }}
      changed_when: true
      ignore_errors: true

    - name: "[NET] Attendre que le bond soit établi (10s)"
      wait_for:
        timeout: 10

    # ════════════════════════════════════════════════════════════════════
    # 3. ROUTES STATIQUES
    # ════════════════════════════════════════════════════════════════════

    - name: "[ROUTE] Appliquer les routes statiques"
      command: "ip route replace {{ item.prefix }} via {{ bond.gateway }} dev {{ bond.name }}"
      loop: "{{ static_routes }}"
      ignore_errors: true
      changed_when: true

    - name: "[ROUTE] Vérifier la table de routage"
      command: ip route show
      register: route_table
      changed_when: false

    - name: "[ROUTE] Afficher les routes"
      debug:
        msg: "{{ route_table.stdout_lines }}"


    # ════════════════════════════════════════════════════════════════════
    # 4. VÉRIFICATION DU BOND
    # ════════════════════════════════════════════════════════════════════

    - name: "[BOND] Vérifier l'état du bond1"
      command: "cat /proc/net/bonding/{{ bond.name }}"
      register: bond_status
      changed_when: false

    - name: "[BOND] Afficher l'état du bond"
      debug:
        msg: "{{ bond_status.stdout_lines }}"

    - name: "[BOND] Confirmer mode 802.3ad"
      assert:
        that: "'802.3ad' in bond_status.stdout"
        fail_msg: "ERREUR : bond1 n'est pas en mode 802.3ad"
        success_msg: "OK : bond1 en mode 802.3ad ✓"

    - name: "[BOND] Compter les membres UP"
      set_fact:
        members_up: "{{ bond_status.stdout | regex_findall('MII Status: up') | length }}"

    - name: "[BOND] Confirmer IP 192.168.2.1 présente"
      command: "ip addr show {{ bond.name }}"
      register: bond_ip
      changed_when: false

    - name: "[BOND] Afficher l'IP"
      debug:
        msg: "{{ bond_ip.stdout_lines }}"

    - name: "[BOND] Confirmer IP"
      assert:
        that: "'192.168.2.1' in bond_ip.stdout"
        fail_msg: "ERREUR : IP 192.168.2.1 absente de bond1"
        success_msg: "OK : IP 192.168.2.1 sur bond1 ✓"

    # ════════════════════════════════════════════════════════════════════
    # 5. TESTS DE CONNECTIVITÉ
    # ════════════════════════════════════════════════════════════════════

#    - name: "[PING] Ping vers la passerelle (192.168.2.254)"
#      command: ping -c 3 -W 2 192.168.2.254
#      register: ping_gw
#      changed_when: false
#      ignore_errors: true

#    - name: "[PING] Résultat ping passerelle"
#      debug:
#        msg: "{{ ping_gw.stdout_lines[-3:] }}"

#    - name: "[PING] Ping vers Machine1 (192.168.1.1)"
#      command: ping -c 3 -W 2 192.168.1.1
#      register: ping_m1
#      changed_when: false
#      ignore_errors: true

#    - name: "[PING] Résultat ping Machine1"
#      debug:
#        msg: "{{ ping_m1.stdout_lines[-3:] if ping_m1.stdout_lines is defined else 'Machine1 non joignable' }}"

    # ════════════════════════════════════════════════════════════════════
    # 6. RÉSUMÉ FINAL
    # ════════════════════════════════════════════════════════════════════

    - name: "[CHECK] Résumé final Machine2"
      debug:
        msg:
          - "════════════════════════════════════════════"
          - "  Machine2 configurée — RÉSUMÉ"
          - "════════════════════════════════════════════"
          - "  Bond          : {{ bond.name }} (802.3ad)"
          - "  Membres       : {{ bond.members | join(' + ') }}"
          - "  Membres UP    : {{ members_up }} / {{ bond.members | length }}"
          - "  IP bond1      : {{ bond.ip }}"
          - "  Passerelle    : {{ bond.gateway }} (SW3/SW4 SVI VLAN20)"
          - "  Route M1      : 192.168.1.0/24 via {{ bond.gateway }}"
          - "  Mgmt OOB      : {{ mgmt_iface }} (DHCP NAT)"
          - "════════════════════════════════════════════"
